import 'package:flutter/material.dart';

class FirstEntreeView extends StatelessWidget {
  const FirstEntreeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FirstEntreeView'),
      ),
    );
  }
}